> **INVALIDATED TOMBSTONE — NOT RUNNABLE.** The retired implementation converted uncalibrated and correlated sequence heuristics into candidate rankings and experimental-routing claims. Git preserves the executable history; the live tree does not.

# comp-030 — DAF SCR1-4 Cassette Ranking

**Status:** Invalidated for candidate selection, cassette ranking, fold or secretion inference, configuration validation, and experimental-routing decisions.

## What is invalidated

No numerical score, rank, candidate count, promoted set, max-CAI preference, direct-secretion preference, GlaA-KEX2 rejection, ESM2 pseudo-pLDDT inference, chaperone-load coefficient, composite verdict, or cross-target generalization survives.

## What survives

Direct secretion and GlaA-KEX2 remain distinct candidate processing routes. Their relative performance for an exact DAF SCR1-4 construct is unresolved. Codon variants may be an independently controlled experimental factor, but this retired ranking supplies no preferred variant.

The [current evidence page](../../../daf-cd55-scr14-cassette-ranking-computational.md) preserves the bounded conjecture and matched experiment. The required readouts are expression, processing fidelity, native-fold attainment, intact secreted product, and retained complement-regulatory function. A result applies only to the exact tested construct × route × codon configuration.

## Hash-bound retirement record

[`invalidation.json`](./invalidation.json) binds every retired non-review file to Git commit `70e60ea9a7c84a92cec37164f38b456aaa6d6881` by byte count and SHA-256. It defines the exact invalidated and surviving scopes and records their canonical digest.

Legacy code, inputs, outputs, figures, provenance, and push reviews are available only through Git history. There is no reproduction command because this tombstone is not an active experiment.
