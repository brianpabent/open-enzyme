# Historical raw API captures. They are intentionally retained unchanged and are
# not reproducible from the sole live script, scripts/scope_validate.py.
# Historical aggregated artifacts (phase-N-*.json, phase-N-*.md) remain committed.
outputs/_lotus_raw/
outputs/_chembl_raw/
outputs/_npatlas_raw/
outputs/_knapsack_raw/
outputs/_chembl_molecule_inchikey_cache.json
outputs/_knapsack_inchikey_cache.json
outputs/_chokepoint_chembl_targets.json
