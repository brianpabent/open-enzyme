ACTION_REQUIRED: no
REVIEWED_SNAPSHOT: 82b5afd1722f656aaa7e7d6f5868bf9cc32b47baaf3e0ab10e3bfa8de1080c00

# Independent post-run review — COMP-014

## Exact snapshot

This context-isolated review covered every entry in the repository-represented
legacy post-run manifest:

- 8 design files;
- 29 committed generated outputs;
- 17 proposed updates;
- 54 files and 13,219,073 bound bytes in total.

Every file hash and byte count matched the manifest and the current repository.
The canonical manifest digest is the reviewed snapshot above.

Gitignored raw captures and local caches are intentionally outside this
snapshot. They are absent from a clean checkout, are not read by the surviving
live validator, and are not required to support any current claim. The
repository retains the committed historical derived outputs only as
non-authoritative leads.

## Verdict

The corrected artifact is valid as a partial retrieval inventory and
primary-source rehydration queue. It does not support candidate rankings,
feasibility verdicts, dose or exposure conclusions, production or chassis
decisions, universal absence claims, or the retired Phase-6 `PURSUE`/`DROP`
logic.

No remaining correction is required for this snapshot.

## Code, inputs, and outputs

The only surviving executable is `scripts/scope_validate.py`. It validates the
retained Phase-1 input structure and regenerates `outputs/scope-summary.md`; it
does not reproduce the historical database pulls, joins, rankings, or
feasibility triage.

Two independent executions were deterministic:

- 18 historical anchor species;
- 19 rendered historical target entries;
- output SHA-256
  `f6e2cf580ea16084a0cf5e5c3f744fecf02a1e435b4962ffb2fb5cdbf889051f`.

The validator handles object-valued and alias-valued bioactivity-source
entries, and all six recorded sources appear in the generated summary. Retired
result-bearing scripts and Phase-6 outputs remain absent.

The committed historical outputs consistently carry the corrected authority
boundary: they nominate records for primary review but cannot establish
mechanism-matched function, exact fungal provenance, safety, exposure,
delivery, or efficacy. Search misses remain query-bounded non-retrievals.

## Proposed interpretations

The 17 bound repository surfaces preserve the same boundary:

- the medicinal-mushroom track stands on its own sourcing, exposure,
  immune-direction, and falsification gates rather than using koji as a foil;
- COMP-014 is not used as primary evidence for ADA, PINK1, C5aR1, or other
  mechanisms;
- cross-track rankings remain on portfolio comparison surfaces;
- the useful H06 exact-material idea survives as a labeled Research Conjecture
  with source-backed premises and a discriminating observation;
- experimental ranges and reagent identities are qualified rather than
  presented as established chassis-achievable conditions.

No downstream surface treats COMP-014 as an intervention-selection result.

## Review limits

This review establishes exact repository integrity, internal consistency,
deterministic scope validation, and fidelity of the proposed interpretations.
It does not rerun the retired retrieval pipeline or independently rehydrate
every historical source row. The artifact itself now makes that limit
explicit: historical rows remain leads until verified against primary records.

## Required actions

None.

ACTION_REQUIRED: no
REVIEWED_SNAPSHOT: 82b5afd1722f656aaa7e7d6f5868bf9cc32b47baaf3e0ab10e3bfa8de1080c00
