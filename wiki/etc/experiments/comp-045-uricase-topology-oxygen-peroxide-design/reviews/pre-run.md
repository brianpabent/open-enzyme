PRE_RUN_GATE: GO
REVIEWED_SNAPSHOT: 43563a3d9660282e9ebd82a954130abcfc880b043ee84f25c9c5bd5f8e308006

# Adversarial pre-run review — comp-045

## Reviewed snapshot

Reviewer `/root/comp045_gate1_4356`; manifest digest `43563a3d9660282e9ebd82a954130abcfc880b043ee84f25c9c5bd5f8e308006`; five design files and two prior-output baselines. The manifest payload, bound-file hashes, sizes, and live design-file set matched.

## Bottom-line verdict

The exact/proposed evidence vocabulary and blocked candidate layout are internally valid for design-only execution. The computation cannot emit a biological outcome, topology ranking, oxygen-sufficiency claim, isolated KatG/VHb attribution, or peroxide-closure claim.

All prior required corrections are closed: vocabulary value sets and the complete 14-contrast mapping are exact-matched; active-UOX retained activity is an operative blocker; the queue and `mkdocs.yml` are audited; and earlier source-scope, attribution, control, determinism, and invalidation defects are resolved.

## Question and model fit

The model answers only whether 18 unique configurations, 20 block assignments, four evidence axes, and the candidate plate layout satisfy their declared contracts.

Exact Gao/PULSE baseline and joint KatG+VHb signatures are separated from Li/Zhao related intracellular precedents. KatG-only and VHb-only rows remain proposed isolation tests. Koji rows have no direct UOX precedent. InaK-N surface accessibility, reaction-site peroxide closure, new concentration activity, and oxygen sufficiency remain unresolved.

## Constraint and implementation audit

The layout closes exactly: ten assignments × two UOX states × four concentrations plus four anchors × four concentrations equals 96 wells per plate. Three runs × two oxygen contexts × two blocks yields 12 plates. Both allowed repeated LamB comparators occur exactly twice; all other configurations occur once.

Every one of the 14 frozen contrasts has its exact test and comparator in the required block. Exact active/inactive UOX pairing includes zero urate. Wet-lab readiness remains blocked on active-UOX retained activity, inactive-control identity and equivalence, support-module function, catalase localization/activity, stocks, normalization, dissolved oxygen, sampling, assay compatibility, and low-concentration quantification.

Allocation is deterministic from a fixed SHA-256 key, and sorted JSON plus the two-run hash procedure provides an adequate reproducibility contract.

## Load-bearing pre-run table

| Planned claim or parameter | Artifact location | Intended implementation use | Provenance status | Verdict |
|---|---|---|---|---|
| Exact versus related precedent | Input tables and validators | Prevent related constructs becoming exact PULSE evidence | Named primary-record identifiers and relationship limits | Pass |
| Joint KatG+VHb attribution | Grading and oxygen-state functions | Prevent isolated-component attribution | Joint-only source scope preserved | Pass |
| Complete categorical vocabulary | Input vocabulary and exact expected sets | Reject added, removed, or substituted states | Fully fail-closed | Pass |
| Fourteen contrasts | Input mapping and frozen expected mapping | Preserve same-block comparisons | Exact mapping and membership checked | Pass |
| Candidate layout | Plate builder | Produce twelve 96-well blocked templates | Deterministic fixed-seed allocation | Pass |
| Wet-lab blocker | Readiness contract | Prevent template being treated as executable protocol | Complete blocker set, including retained active-UOX | Pass |
| Historical output replacement | Schema migration contract | Prevent stale result-bearing interpretation | Schema 2 explicitly replaces prior outputs | Pass |

## Falsification, sensitivity, and output contract

Contrary biological results are outside this computation because no measurements enter it. The only permitted disposition is `CANDIDATE_LAYOUT_GENERATED`, with `NOT_EVALUATED` and blocked wet-lab readiness.

Planned outputs expose configuration-level provenance, oxygen-specific evidence states, all contrasts, complete plate maps, control definitions, blockers, readouts, and limitations. Concentration and oxygen labels remain experimental regimes, not efficacy estimates or probabilities.

## Downstream authoring contract

The canonical evidence home and direct dependents are named. The active synthesis queue is explicitly scheduled for deletion only after corrective propagation and Gate 2 closure. `mkdocs.yml` is correctly recorded as navigation-only.

The invalidation boundary is narrow: future evidence can affect only the tested construct × concentration × oxygen × control regime. It cannot eliminate neighboring topologies, gut-lumen UOX generally, or the project mission.

## Required actions before execution

None.

## Review limits

Static inspection only. `analyze.py` was not executed. Historical outputs were inspected only for schema compatibility. Per instruction, no browsing or independent primary-source re-verification was performed.
